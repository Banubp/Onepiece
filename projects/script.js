const bar=document.querySelector('.progress i'),sections=[...document.querySelectorAll('.reveal')],reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;function progress(){const max=document.documentElement.scrollHeight-innerHeight;bar.style.width=`${max?scrollY/max*100:0}%`}addEventListener('scroll',progress,{passive:true});progress();if(!reduced){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.animate([{opacity:0,transform:'translateY(22px)'},{opacity:1,transform:'none'}],{duration:500,easing:'cubic-bezier(.2,.8,.2,1)',fill:'both'});observer.unobserve(entry.target)}}),{threshold:.08});sections.forEach(section=>observer.observe(section))}
